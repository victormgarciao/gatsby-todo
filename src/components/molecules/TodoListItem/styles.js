export const getStyles = () => ({
    segment: { margin: `0.5rem`, cursor: `pointer` },
    listItem: {
        display: `flex`,
        justifyContent: `space-between`,
        alignItems: `center`,
    }
})